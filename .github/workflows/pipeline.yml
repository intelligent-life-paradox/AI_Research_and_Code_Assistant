<<<<<<< HEAD
name: Diffusion Models AI Pipeline
=======
name: Diffusion Models With Help of AI Pipeline
>>>>>>> 70ef76879d53bbede125920ffabaef46a9ec05a2

on:
  push:
    branches: [main]
<<<<<<< HEAD
  workflow_dispatch:

jobs:
  # Job anterior + atualização: Testa a logica Python (Ingestao, Indexacao, Execucao)
  pipeline:
    runs-on: ubuntu-latest
    env:
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python dependencies
        run: |
          pip install -r crew_diffusion/requirements.txt
          pip install arxiv gitpython llama-index llama-index-vector-stores-chroma chromadb

      - name: Ingest and Index Knowledge
        run: python rag/pipeline_manager.py --mode all

      - name: Run CrewAI Agent
        env:
          USER_INPUT: "Explain the forward diffusion process in DDPM"
        run: cd crew_diffusion/src && python -m crew_diffusion.main

  # novo job: verificar se a aplicacao pode ser containerizada com sucesso
  docker-build-check:
    runs-on: ubuntu-latest
    
    needs: pipeline
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build . --file Dockerfile --tag diff-crew-app:latest
=======
  workflow_dispatch:  # trigger this thing

jobs:
  pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' #Modify it for your own enviroment

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Step 1 - Ingest knowledge
        run: |
          python ingest_knowledge.py

      - name: Step 2 - Index PDFs
        run: |
          jupyter nbconvert --to notebook --execute rag/index_pdfs.ipynb

      - name: Step 3 - Index code repositories
        run: |
          jupyter nbconvert --to notebook --execute rag/index_codes.ipynb

      - name: Step 4 - Run crew
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          USER_INPUT: "Explain the forward diffusion process in DDPM"
        run: |
          cd src && python -m crew_diffusion.main
>>>>>>> 70ef76879d53bbede125920ffabaef46a9ec05a2
